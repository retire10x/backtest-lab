# backtest-lab — Claude Code 작업 지침

이 프로젝트는 Cowork(웹/데스크톱 Claude)와 Claude Code(터미널)가 번갈아
작업을 이어가는 프로젝트입니다. 서로 실시간으로 대화할 수 없으므로,
`docs/dev_log.md` 파일을 우편함처럼 씁니다.

## 작업 시작 전 필수

1. `docs/dev_log.md`를 열어 맨 위(최신) 항목부터 읽는다 — 지금까지 뭐가
   됐고, 뭐가 남았고, 상대방이 남긴 질문/요청이 있는지 확인.
2. 아래 "반드시 지킬 규칙"을 확인.
3. 작업을 마치면 `docs/dev_log.md` 맨 위에 새 항목을 추가한다 (형식은
   해당 파일 하단 참고).

## 프로젝트 배경

소액(100만원 단위) 개인 투자자를 위한 무료 정보 사이트. 코스피200 ETF와
국채3년물 ETF의 12개월 수익률을 비교해 더 강한 자산을 표시하는 듀얼
모멘텀 전략의 **신호와 백테스트 결과만** 보여준다. 실행(매수/매도)은
사용자가 본인 계좌에서 직접 판단해서 한다 — 이 사이트나 별도 프로그램이
대신 주문을 넣지 않는다.

배경 문서: `신규프로젝트_개미투자정보사이트_킥오프프롬프트.md` (규제 검토
포함), `docs/dev_log.md` (진행 이력).

## 반드시 지킬 규칙

1. **1:1 상담/개별 조언 기능을 절대 만들지 않는다.** 모든 콘텐츠는
   불특정 다수에게 동일하게 노출되는 방송형 콘텐츠여야 한다.
2. **실시간 매매 리딩(지금 사세요/파세요) 기능을 만들지 않는다.**
3. **성과/신호를 다루는 페이지에는 반드시 면책 문구를 넣는다.**
   (`src/components/Disclaimer.astro` 재사용)
4. **개인 계좌 정보(잔고, 보유수량, 실거래 기록)를 이 프로젝트의 DB나
   코드에 절대 넣지 않는다.** 공개 DB에는 "신호"와 "백테스트 성과"만
   들어간다.
5. **WorkerAI(`D:\develop\WorkerAI`) 코드를 import하거나 그대로
   복사하지 않는다.** 로직만 참고해서 이 프로젝트 안에서 새로
   구현한다 — 두 프로젝트는 코드/실행환경을 공유하지 않는 것이 원칙.
6. 방향을 바꾸는 결정(예: 자산군 변경, 유료화 시점, 무료→유료 전환,
   리스크 기준 변경, 새 규제 이슈 발견)은 **사용자 승인 없이 진행하지
   않는다.** 확신이 안 서면 진행하지 말고 먼저 묻는다. 애매하면
   `docs/dev_log.md`에 "확인 필요"로 남겨두고 중단해도 된다.

## 기술 스택 (확정)

- **프론트/사이트**: Astro (`output: 'server'`, `@astrojs/vercel` 어댑터)
- **DB**: Supabase (Postgres, 무료 티어) — anon 키는 읽기 전용(RLS로
  강제), 쓰기는 service_role 키를 쓰는 별도 업데이트 프로그램만.
- **호스팅**: Vercel (무료)
- 이유/대안은 이 대화 이력 또는 `docs/dev_log.md` 참고.

## 현재 상태 (2026-08-11 기준 — 최신 상태는 dev_log.md 확인)

완료:
- `supabase/schema.sql` — `monthly_signals`, `backtest_summaries`,
  `walk_forward_results` 테이블 + 공개 읽기 RLS 정책
- 페이지 4개: `/`, `/strategy/dual-momentum`, `/signal`, `/backtest`
- 로컬 빌드/렌더링 검증 완료 (Supabase 미연결 상태에서도 정상 동작)

미완료:
- 실제 Supabase 프로젝트 생성 및 `.env` 키 연결
- 월 1회 신호를 계산해 DB에 upsert하는 업데이트 스크립트 (별도 프로그램)
- Vercel 배포 및 도메인 연결
- SEO 콘텐츠 확장

## 로그 작성 규칙 (`docs/dev_log.md`)

새 항목은 파일 **맨 위**에 추가한다(최신이 위로). 형식:

```
## YYYY-MM-DD HH:MM (작성자: Claude Code | Cowork)
**한 일**: ...
**왜**: ...
**결과/남은 이슈**: ...
**다음 담당자에게**: (질문·확인 요청이 있으면 여기에, 없으면 생략)
```
